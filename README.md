# Stormy V3.0 — Full Working Version

Real LLM + voice + tools + Docker.

1. `cp .env.example .env` and add your API key
2. `docker compose up --build`
3. Open http://localhost:8000

Photo background, full personality, real LLM responses.
